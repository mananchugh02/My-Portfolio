import React from "react";
import {Tilt} from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className=' rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-[#fffff] text-[17px] max-w-3xl leading-[30px]'
      >
        I am Manan Chugh, a student at the Indraprastha Institute of Information 
        Technology Delhi (IIIT Delhi), pursuing a Bachelor of Technology (B.Tech) 
        in Computer Science and Design (CSD). I am a diligent and hardworking student 
        who is passionate about computer science and design. I have a keen interest in 
        exploring the intersection of these two fields and have been actively involved 
        in various projects and activities that showcase my skills and creativity. I am 
        a quick learner and have a strong foundation in programming, data structures, 
        algorithms, and design principles. I am also an excellent team player and have 
        collaborated with my peers on several projects. With my strong technical skills and 
        creative mindset, I am well-equipped to take on the challenges of the tech industry.
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
